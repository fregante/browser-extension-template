name: Test

on:
  - pull_request
  - push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm test
      - run: echo $GITHUB_RUN_ID
      - run: 'false'
      - run: sleep 200
        if: ${{ always() }}

